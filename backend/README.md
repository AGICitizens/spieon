# spieon-backend

FastAPI service. Python 3.12+, managed with [uv](https://docs.astral.sh/uv/).

```bash
uv sync
uv run uvicorn app.main:app --reload
```

OpenAPI schema: `http://localhost:8000/openapi.json` (consumed by frontend codegen).
